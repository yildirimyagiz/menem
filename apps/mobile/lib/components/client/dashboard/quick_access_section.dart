import 'package:flutter/material.dart';

class QuickAccessSection extends StatelessWidget {
  const QuickAccessSection({super.key});

  @override
  Widget build(BuildContext context) {
    // Replace with your actual Quick Access UI
    // Example: A GridView or Row of cards/buttons
    return const Card(
      child: ListTile(
          title: Text("Quick Access Items (e.g., Add Property, View Tenants)")),
    );
  }
}
